import { NextResponse } from "next/server";
import { customerFromRequest, safeCustomer } from "@/lib/customerAuth";
import prisma from "@/lib/prisma";
export const runtime="nodejs";export const dynamic="force-dynamic";
const bad=(message,status=422)=>NextResponse.json({message},{status});
const addressOut=a=>({id:a.uuid,uuid:a.uuid,title:a.label[0].toUpperCase()+a.label.slice(1),label:a.label,name:a.name,phone:a.phone,street:a.address,address:a.address,city:a.city,state:a.state,pincode:a.pincode,is_default:a.isDefault,isDefault:a.isDefault});
export async function GET(request){const customer=await customerFromRequest(request);if(!customer)return NextResponse.json({message:"Unauthenticated"},{status:401});const [orders_count,addresses]=await Promise.all([prisma.order.count({where:{customerUuid:customer.uuid}}),prisma.customerAddress.findMany({where:{customerUuid:customer.uuid},orderBy:[{isDefault:"desc"},{createdAt:"desc"}]})]);const list=addresses.map(addressOut);return NextResponse.json({...safeCustomer(customer),address:list,addresses:list,orders_count});}
export async function PUT(request){const customer=await customerFromRequest(request);if(!customer)return bad("Unauthenticated",401);const body=await request.json();const name=String(body.name||"").trim();const email=String(body.email||"").trim().toLowerCase();const phone=String(body.phone||"").trim();if(!name||name.length>120)return bad("Please enter a valid full name");if(!/^\S+@\S+\.\S+$/.test(email)||email.length>255)return bad("Please enter a valid email address");if(phone&&!/^[0-9+()\s-]{8,20}$/.test(phone))return bad("Please enter a valid phone number");const exists=await prisma.customer.findFirst({where:{email,NOT:{uuid:customer.uuid}},select:{uuid:true}});if(exists)return bad("This email address is already in use",409);const updated=await prisma.customer.update({where:{uuid:customer.uuid},data:{name,email,phone:phone||null},select:{uuid:true,name:true,email:true,phone:true}});return NextResponse.json({message:"Profile updated successfully",data:safeCustomer(updated)});}
